pipeline {
  agent any
  tools { nodejs "nodejs" }
  stages{
    stage("Build Application") {
       steps {
          sh 'npm build'
       }
    }
    stage("Deploy Application") {
       steps {
        sh 'ng deploy'
      }
    }
  }
}
